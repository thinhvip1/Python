def pattern(lines, columns, size):
    
def main():
    t = int(input())
    for i in range(t):
        lines, columns, size = map(int, input().split())
        pattern(lines, columns, size)
        print("")

main()